{
  "name": "openalarm-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "engines": { "node": ">=22.12" },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource/nunito": "^5.2.5",
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
